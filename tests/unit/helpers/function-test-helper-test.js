import { functionTestHelper } from '../../../helpers/function-test-helper';
import { module, test } from 'qunit';

module('Unit | Helper | function test helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = functionTestHelper(42);
  assert.ok(result);
});
